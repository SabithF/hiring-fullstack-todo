# Fullstack Todo App — Server

This is the backend REST API for the Fullstack Todo App.  
It is built using **Node.js**, **Express**, and **MongoDB** and provides all CRUD operations for managing tasks.

---

## Requirements

- Node.js 18+
- MongoDB (Local or MongoDB Atlas)
- npm or yarn

---

## Setup Instructions

- Create the `.env` file inside the **server** folder, and paste:


PORT=5000
DB_URI=mongodb+srv://sabithfariq:typebadmin@todo.gn2dyv1.mongodb.net/?appName=todo


- Run command "npm run dev"
  
